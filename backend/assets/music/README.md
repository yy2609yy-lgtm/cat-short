# Music bed

`cozy_afternoon.wav` is an **original** short loop generated by
`backend/scripts/generate_bed.py`. It is released as **CC0 / public domain**
for this personal workbench (no third-party samples).

The render pipeline loops and ducks it under the source audio.

If the file is missing at container start, the entrypoint regenerates it.
